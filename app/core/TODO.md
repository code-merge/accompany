## Todo for the core system stuff.

- [ ] Next up, if you'd like: we can add logging hooks for rendering time, template cache usage, and missing translation keys. This is already shaping up to be an industrial-grade system

- [ ] Done with the login functionality using cookie based auth in db. But now I need a clean way for the seeding of the required tables at the onboarding.
- [ ] Or see if i can use the ORM to generate the tables instead of the raw query.